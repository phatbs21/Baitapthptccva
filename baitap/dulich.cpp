#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N, T, v; //1 <= v <= N <= 1000, 0 < T < 1000
    cin >> N >> T >> v;
    int t[1001];
    for (int i=1;i<=N;i++)
    {
        cin>>t[i];
    }
    int dem=0;
    int a[100]
    while ()
}