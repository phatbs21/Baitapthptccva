#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, k;
    cin >> n >> k;
    vector<int> a(n);
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    vector<vector<int>> f(n + 1, vector<int>(n + 1, 0)), m(n + 1, vector<int>(n + 1, 0)), v(n + 1, vector<int>(n + 1, 0));
    for(int i=1;i<=n;i++)
    {
        for(int j=0;j<=)
    }
}