#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, k;
    cin >> n >> k;
    int  vt1,vt2;
    cin>>vt1>>vt2;
    vector<vector<int>> a(n, vector<int>(n, 0));

    const int q[] = {-1, 1, 0, 1, 0, -1, -1, 1};
    const int w[] = {-1, -1, 1, 0, -1, 0, 1, 1};
    for (int i = 0; i < 8; i++)
    {
        for (int j = 0; j < 8; j++)
        {
            
        }
    }
}