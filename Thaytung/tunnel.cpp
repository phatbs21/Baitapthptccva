#include <bits/stdc++.h>
using namespace std;
#define Name "tunnel"
ifstream fi(Name ".inp");
ofstream fo(Name ".out");
int main()
{
    int n;
    fi >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++)
        cin >> a[i];
}