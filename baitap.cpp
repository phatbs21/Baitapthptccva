#include <bits/stdc++.h>
using namespace std;
int falseWords(std::vector<string> words)
{
    c
}
int main()
{
    vector<string> V;
    int i = 6;
    int input;
    while (i--)
    {
        cin >> input;
        V.push_back(input);
    }
    cout << falseWords(V);
}
