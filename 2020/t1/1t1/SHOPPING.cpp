//bo bai
#include <bits/stdc++.h>
using namespace std;
int n;
int cax = 0;
int tong = 0;
int backtracking(int i)
{
    if (i > n)
    {
        if (tong > cax)
            tong = cax;
    }
    for (int j = 1; j <= n; j++)
    {
    }
}
int main()
{
    freopen("SHOPPING.out", "w", stdout);

    cin >> n;
    int x;
    cin >> x;
    int a[n + 1];
    for (int i = 1; i <= n; i++)
    {
        cin >> a[i];
    }
}