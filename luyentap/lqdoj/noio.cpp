#include <bits/stdc++.h>
using namespace std;
vector<vector<int>> a;

int main()
{
    int n, m;
    cin >> n >> m;
    a.assign(n, vector<int>(m, 0));
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++)
            cin >> a[i][j];
    int q, p;
    cin >> q >> p;
    for (int i = 0; i < q; i++)
    {
        int x1, x2, y1, y2;
        cin >> x1 >> y1 >> x2 >> y2;
        
    }
}