#include <bits/stdc++.h>
using namespace std;
vector<int> c;
vector<vector<int>> a;

int main()
{
#ifndef ONLINE_JUDGE
    freopen("CJTHAMQUAN.inp", "r", stdin);
#endif
    int n, m;
    cin >> n >> m;
    c.assign(n + 1, 0);
    //a.assign(n+1);
    for(int i=0;i<m;i++)
    {
        
    }
}