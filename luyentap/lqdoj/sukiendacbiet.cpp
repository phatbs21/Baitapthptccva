#include <bits/stdc++.h>
using namespace std;
#define it long long int
int so[] = {6, 2, 5, 5, 4, 5, 6, 3, 7, 6};
it kt(it vach, it chuso)
{
    //y+z+t+p+q
    if (vach)
}
int main()
{
    string s;
    cin >> s;
    if (s == "1")
    {
        cout << "NO SOLUTION";
        return 0;
    }
    string s1;
    int n = s.length() - 1;
    if(s[n]==48)
    {
        s[n]=54;
        cout<<s;
        return 0;
    }
    if(s[n]==50)
}