#include <bits/stdc++.h>
using namespace std;
int dfs(int u)
{
}
int main()
{
    int n, sy, sx, dy, dx;
    cin >> n >> sy >> sx >> dy >> dx;
    int a[100][100];
    a[sy][sx] = 1;
    a[dy][dx] = 0;
    for (int i = 0; i < n; i++)
    {
    }
}