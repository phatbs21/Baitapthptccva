#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    int l = 1, r = n;
    while (l <= r)
    {
        int m = l + (r - l) / 2;
        while (l <= r)
        {
            
        }
    }
}